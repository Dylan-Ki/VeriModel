# Core dependencies (bắt buộc cho Vercel)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
jinja2>=3.1.0
python-multipart>=0.0.6

# CLI dependencies (có thể bỏ qua trên Vercel nếu cần giảm size)
rich>=10.0.0
typer>=0.9.0

# Scanner dependencies
yara-python>=4.5.0
# docker>=7.0.0  # ❌ BỎ: Không cần thiết trên Vercel (không có Docker)

# Threat Intelligence
requests>=2.31.0

# Safetensors converter (TÙY CHỌN - có thể bỏ để giảm size)
# safetensors>=0.4.0
# torch>=2.0.0  # ⚠️ RẤT NẶNG (~500MB+) - chỉ bật nếu thực sự cần

# ============================================
# KHUYẾN NGHỊ CHO VERCEL DEPLOYMENT:
# ============================================
# 1. BẮT BUỘC: fastapi, uvicorn, jinja2, python-multipart, yara-python, requests
# 2. TÙY CHỌN: rich, typer (cho CLI - không cần thiết trên web)
# 3. BỎ QUA: docker (không hỗ trợ)
# 4. CÂN NHẮC: safetensors, torch (rất nặng, có thể disable converter trên Vercel)

# Để deploy nhẹ nhất (CHỈ STATIC SCAN + UI):
# Uncomment phần dưới và comment phần trên
# fastapi>=0.104.0
# uvicorn[standard]>=0.24.0
# jinja2>=3.1.0
# python-multipart>=0.0.6
# yara-python>=4.5.0
# requests>=2.31.0
