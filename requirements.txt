# Core dependencies (bắt buộc)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
jinja2>=3.1.0
python-multipart>=0.0.6

# CLI dependencies
rich>=10.0.0
typer>=0.9.0

# Scanner dependencies
yara-python>=4.5.0
docker>=7.0.0

# Threat Intelligence
requests>=2.31.0

# Safetensors converter (tùy chọn - có thể bỏ qua nếu không cần convert)
safetensors>=0.4.0
# torch>=2.0.0  # Bỏ comment này nếu cần chuyển đổi safetensors (có thể cần cài đặt riêng trên Windows)